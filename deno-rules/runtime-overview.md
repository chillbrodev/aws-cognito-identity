# Deno Runtime Overview

Deno is a modern runtime for JavaScript and TypeScript that provides:

- Built-in TypeScript support
- Web Standard APIs
- Secure by default (explicit permissions)
- No package manager or node_modules
- Built-in tooling (formatter, linter, test runner)
