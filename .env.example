# Copy to .env and fill in with your Cognito values (do not commit .env).
# Then run: deno task test:live
# Or: deno run --allow-net --allow-env scripts/test_cognito_live.ts

COGNITO_USER_POOL_ID=
COGNITO_CLIENT_ID=
COGNITO_TEST_USERNAME=
COGNITO_TEST_PASSWORD=
COGNITO_REGION=